// Given a random non-negative number, you have to return the digit of this number within an array in reverse ondragover.
function digitize(n) {
    const digitize = n.reverse(); 
    return digitize
  }
  console.log(digitize([9, 8, 7, 6, 5, 4, 3, 2, 1]))